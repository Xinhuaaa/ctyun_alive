# 天翼云电脑保活接口文档

## 概述

本项目通过以下接口实现天翼云电脑的自动保活功能。

## 使用的接口列表

### 1. 主页面访问
- **URL**: `https://pc.ctyun.cn`
- **方法**: GET
- **用途**: 访问天翼云电脑主页面，用于初始化登录和云电脑管理
- **代码位置**: 
  - `run.py` 第74行
  - `run_first.py` 第20行

### 2. 获取验证码接口
- **URL**: `https://desk.ctyun.cn:8810/api/auth/client/captcha`
- **方法**: GET
- **参数**:
  - `height`: 验证码图片高度 (36)
  - `width`: 验证码图片宽度 (85)
  - `userInfo`: 用户账号
  - `mode`: 模式 (auto)
  - `_t`: 时间戳（毫秒）
- **用途**: 获取图形验证码图片，用于登录验证
- **代码位置**: `utils/captcha.py` 第9行
- **返回**: 验证码图片内容（二进制）

### 3. 用户登录接口
- **URL**: `desk.ctyun.cn:8810/api/auth/client/login`
- **方法**: POST（通过页面监听）
- **用途**: 用户登录认证接口
- **代码位置**: `run.py` 第42行
- **监听方式**: 使用 `page.listen.start()` 监听网络请求
- **返回字段**: 
  - `tenantName`: 租户名称（登录成功）
  - 错误信息：
    - "验证码错误"
    - "图形验证码错误"
    - "请输入图形验证码"

### 4. 云电脑列表接口
- **URL**: `desk.ctyun.cn:8810/api/desktop/client/pageDesktop`
- **方法**: GET/POST（通过页面监听）
- **用途**: 获取用户的云电脑列表信息
- **代码位置**: `run_first.py` 第21行
- **监听方式**: 使用 `page.listen.start()` 监听网络请求
- **返回字段**: 包含 "云电脑租户" 关键字表示成功

## 保活机制说明

### 工作流程

1. **登录流程**:
   - 访问 `https://pc.ctyun.cn`
   - 检测是否需要登录
   - 如需登录，输入账号密码
   - 如有验证码要求，调用验证码接口获取并识别
   - 提交登录表单，监听登录接口响应

2. **保活流程**:
   - 定位云电脑列表中的云电脑项 (`.desktopcom-item-main`)
   - 根据配置的 `TARGETS` 选择要保活的云电脑
   - 点击云电脑项
   - 点击"进入"按钮 (`.desktopcom-enter`)
   - 保持云电脑页面打开 `ALIVE_SECOND` 秒（默认80秒）
   - 返回云电脑列表
   - 重复以上步骤处理其他需要保活的云电脑

3. **定时任务**:
   - 使用 `schedule` 库实现定时执行
   - 默认每 `INTERVAL_MINUTE` 分钟执行一次（默认45分钟）
   - 启动时立即执行一次保活任务

### 关键参数配置

- `INTERVAL_MINUTE`: 保活任务执行间隔（分钟），建议不超过60分钟
- `ALIVE_SECOND`: 云电脑页面保持打开时间（秒），建议不少于80秒
- `TARGETS`: 需要保活的云电脑编号列表，-1表示全部

## 技术实现

### 使用的技术栈
- **DrissionPage**: 用于浏览器自动化操作
- **ddddocr**: 用于验证码识别
- **requests**: 用于HTTP请求（验证码获取）
- **schedule**: 用于定时任务调度

### 浏览器自动化
- 使用无头浏览器模式（Linux环境）
- 支持代理服务器配置
- 保存浏览器会话数据以避免重复验证

### 验证码处理
- 自动获取图形验证码
- 使用OCR技术自动识别验证码
- 自动填充验证码到登录表单

## 注意事项

1. **网络监听**: 项目通过监听特定接口的网络请求来判断操作是否成功
2. **会话保持**: 通过保存浏览器数据文件夹（`data`）来保持登录状态
3. **防封策略**: 建议保活间隔不要设置得过于频繁
4. **验证码**: 如果首次在新设备登录需要短信验证，需要先运行 `run_first.py` 手动登录一次
